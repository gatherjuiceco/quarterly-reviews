# Quarterly Reviews

Personal quarterly review archive, served via GitHub Pages.

Each quarter is a self-contained HTML file (`q1-2026.html`, `q2-2026.html`, ...). `index.html` links them all — add a card there when adding a new quarter.

Not indexed by search engines (`robots.txt` + `noindex` meta), but the site is public: anyone with the link can view it.
